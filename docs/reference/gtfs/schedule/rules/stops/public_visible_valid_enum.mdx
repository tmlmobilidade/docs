---
title: Regra de verificação de visíblidade
description: Regra
---
## Regras de validação do campo public_visible


### Campos que serão validados nesta regra

| Nome do Campo | Tipo                                         | Presença    | Descrição                                                                                                 |
| ------------- | -------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |




### O que esta validação faz 

O `public_visible` indica se a paragem é visível ao público ou não, através dos códigos 0 (não) e 1 (sim). O campo é opcional; deixar o campo vazio é aceite no standard — significa não fornecer essa informação. O validador aplica as regras do projecto sobre presença, proibição, valores permitidos (apenas 0 e 1) e, se existir, lista de opções permitidas.

---

#### Passa

- O valor vem preenchido e é 0 ou 1, de acordo com o significado previsto.
- O campo fica vazio e a regra dispensa a verificação (por exemplo, severidade de ignorar), sem exigir o preenchimento neste passo.
- O valor está entre as opções permitidas pelas regras (ou a regra aceita todas as opções).
- O valor veio fornecido e o atributo não está proibido pelas regras.

---

#### Falha

- Valor em falta — o campo fica vazio, quando, para a severidade e regras, o preenchimento é obrigatório ou recomendado (o standard admite vazio, mas a política de dados exige a indicação).
- Proibido — as regras não permitem o `public_visible`, mas o valor veio no mesmo fornecido.
- Inválido — um valor que não seja 0 nem 1.
- Fora do permitido — o valor é 0 ou 1, mas não consta do conjunto de opções definido nas regras (salvo quando a regra abrange todas as opções).


