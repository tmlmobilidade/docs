---
title: Export Data
description: Ferramenta de exportação de dados
---
Esta aplicação é a ferramenta oficial de exportação de dados da Transportes Metropolitanos de Lisboa. Permite que utilizadores autorizados extraiam dados operacionais e analíticos da plataforma GO e os guardem em ficheiros CSV para relatórios, análise ou utilização noutros sistemas.

O acesso é controlado através de uma chave de acesso que é introduzida ao iniciar a ferramenta.

## Como correr a ferramenta de exportação?

É necessário ter instalado o (Node.js)[https://nodejs.org/en/download].

1. Na linha de comandos (*Powershell no Windows*, ou *Terminal no Mac*) correr o seguinte comando

```npm
npx @tmlmobilidade/export-data
```

2. Pode ser necessário instalar a ferramenta, aqui confirmamos

```bash
❯ npx @tmlmobilidade/export-data
Need to install the following packages:
@tmlmobilidade/export-data@20260213.1500.47
Ok to proceed? (y) y
```

3. A primeira vez que corremos a ferramenta é nos pedida a chave de acesso, esta apenas é pedida uma vez e depois fica guarda no sistema.

   ```bash
◆  Por favor introduz a chave de acesso:
│  Chave de acesso...
└ 
   ```

4. A ferramenta vai pedir para escolher o tipo de exportação que deseja. Aqui podemos usar as setas do teclado para navegar pelas opções e pressionar no espaço para selecionar a opção e no enter para confirmar.

```bash

    ▒▒▒▒▒▒▒▒▒   ▒▒▒▒▒▒▒▒   ▒▒▒▒▒▒▒▒
  ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
▒▒▒▒▒▒▒  ▒▒▒▒▒▒▒▒▒▒  ▒▒▒▒▒▒▒▒▒  ▒▒▒▒▒▒
▒▒▒▒▒       ▒▒▒▒▒      ▒▒▒▒▒     ▒▒██▒
▒▒▒▒         ▒▒▒       ▒▒▒▒      ▒████▒
▒▒▒▒▒                             ▒██▒
▒▒▒▒▒▒▒ ▒██▒
  ▒▒▒▒▒▒████▒
    ▒▒▒▒▒██▒

┌  Bem-vindo ao exportador de dados da CM!
│
●  A versão da aplicação é: 20260213.1500.47
│
●  O ID desta exportação é: 7DTHDZ
│
●  Todos os resultados serão guardados aqui: /Users/josue.monteiro/export-7DTHDZ <-- Este é o local onde serão guardados os resultados da exportação.
│
◇  Chave de acesso encontrada no armazenamento seguro do sistema.
│
◆  Escolhe os dados que queres exportar:
│  ◻ 1. Validações APEX
│  │ ◻ 1.0. Validações em bruto
│  └ ◻ 1.1. Validações agregadas (escolher campos)
│  ◻ 2. Rides
│  └ ◻ 2.0. Rides em bruto (SLAs)
│  ◻ 3. Vehicle Events
│  └ ◻ 3.0. Vehicle Events em bruto
│  ◻ 4. HashedShapes
│  └ ◻ 4.0. HashedShapes para GeoJSON
│  ◻ 5. SAMs
│  └ ◻ 5.0. SAMs em bruto (Sequencialidade)
└
```

## Tipos de dados que podem ser exportados

1. Validações APEX

- **Validações (raw)**
Cada validação de título individual (cada vez que um passageiro valida um passe). Utilizar quando são necessários dados detalhados ao nível de registo individual.

- **Validações (agregadas)**
Os mesmos dados de validação resumidos por data e pelas dimensões escolhidas (por exemplo, linha, paragem, veículo, operador, viagem, tipo de título). Utilizar para relatórios e KPIs (por exemplo, validações por linha por dia) sem ter de lidar com ficheiros raw volumosos.

Ver abaixo mais detalhes de como exportar validações agregadas.

2. Rides

- Rides (raw)
Dados raw de viagens/serviço. Utilizados para análise ao nível de serviço e de tipo SLA (por exemplo, compreender como as viagens foram executadas num período).

3. Eventos de veículos

- Eventos de veículos (raw)
Eventos raw provenientes de veículos (por exemplo, em paragens). Utilizados para análise operacional de como os serviços foram efetivamente operados no terreno.

4. HashedShapes

- HashedShapes para GeoJSON
Formas geográficas de percursos (por exemplo, traçados de linhas) exportadas como GeoJSON. Utilizar quando é necessário representar linhas num mapa ou utilizar os dados em ferramentas GIS ou de mapeamento. Especificam-se as formas a exportar pelos seus IDs.

5. SAMs

- SAMs (raw)
Dados raw de SAM (Sequencialidade): informação sobre sequências de transações (por exemplo, transações esperadas vs encontradas vs em falta). Utilizados para análise de sequência e consistência, tipicamente por operador.


## Exemplo de extração de Validações Agregadas

1. Selecionar (com a barra de espaços) a opção de validações agregadas.
```bash
◆  Escolhe os dados que queres exportar:
│  ◻ 1. Validações APEX
│  │ ◻ 1.0. Validações em bruto
│  └ ◼ 1.1. Validações agregadas (escolher campos)
```

2. Escolher que colunas queremos no nosso ficheiro final CSV, neste exemplo vamos selecionar Linha, Tipo de Passe e Paragem
```bash
◆  Escolhe os campos para agrupar as validações (a data é sempre incluída):
│  ◼ Linha (line_id)
│  ◻ Pattern (pattern_id)
│  ◼ Tipo de passe (product_id)
│  ◻ Viagem (trip_id)
│  ◼ Paragem (stop_id)
│  ◻ Operador (agency_id)
│  ◻ Veículo (vehicle_id)
└
```
3. De seguida escolhemos que filtros queremos aplicar à nossa procura
```bash
◆  Escolhe os filtros que queres aplicar:
│  ◻ Operador (agency_id)
│  ◼ Paragem (stop_id)
│  ◼ Linha (line_id)
│  ◻ Pattern (pattern_id)
│  ◻ Veículo (vehicle_id)
└
```
Neste exemplo vamos filtrar por Line ID, para isso vamos introduzir os IDs separados por vírgulas. Vamos filtrar por duas linhas, a 1001 e a 4701.

```bash
◇   ───────────────────────────────────────────────────────────────────────────╮
│                                                                              │
│  FILTRAR POR LINE ID:                                                        │
│    • Introduz os Line IDs separados por vírgulas. Exemplo: 1001,1002,etc...  │
│    • Se não introduzires nenhum Line ID, este filtro não será aplicado.      │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────╯
│
◆  Line IDs:
│  1001,4701
└
```